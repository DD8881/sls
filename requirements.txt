python-dotenv==1.2.2
python-telegram-bot==22.7
requests==2.34.2
curl_cffi==0.15.0
# Local dev only: web_server.py (static + /api/feedback). Production uses the Worker.
Flask==3.1.2
# promo.py: банери + 9:16 відео (Reels/TikTok). imageio-ffmpeg привозить ffmpeg.
Pillow==12.2.0
imageio==2.37.3
imageio-ffmpeg==0.6.0
numpy==2.4.6
